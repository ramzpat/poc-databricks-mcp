# Example MCP server configuration for Databricks (OAuth-only shared service principal)
warehouse:
  host: ${DATABRICKS_HOST}
  http_path: ${DATABRICKS_HTTP_PATH}
  warehouse_id: ${DATABRICKS_WAREHOUSE_ID}
auth:
  oauth:
    client_id: ${DATABRICKS_CLIENT_ID}
    client_secret: ${DATABRICKS_CLIENT_SECRET}
    token_url: ${DATABRICKS_TOKEN_URL}
scopes:
  catalogs:
    main:
      schemas:
        - default
limits:
  # Use -1 to represent no limit; otherwise set explicit caps.
  max_rows: 10000
  sample_max_rows: 1000
  query_timeout_seconds: 60
  max_concurrent_queries: 5
  allow_statement_types:
    - SELECT
observability:
  log_level: info
  propagate_request_ids: true
metadata:
  # Enable static metadata loading from CSV files
  enabled: false
  # Directory containing metadata files (relative or absolute path)
  directory: ./metadata
